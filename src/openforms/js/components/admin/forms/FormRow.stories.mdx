import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import Field from './Field';
import Fieldset from './Fieldset';
import FormRow from './FormRow';
import {TextInput} from './Inputs';

export const FormDecorator = Story => (
  <form>
    <Fieldset>
      <Story />
    </Fieldset>
  </form>
);

<Meta title="Admin/Django/FormRow" component={FormRow} decorators={[FormDecorator]} />

# Form row

The form row acts as a container for one or more nested fields. This component cannot really be used
by itself, but is intended to be composed with the `Fieldset` component for proper alignment of form
field error lists.

## Single field

<Canvas>
  <Story name="Single field">
    <FormRow fields={['field1']}>
      <Field errors={['Foo']} name="field1" label="Input field" helpText="Lorem ipsum">
        <TextInput />
      </Field>
    </FormRow>
  </Story>
</Canvas>

## Multiple fields

<Canvas>
  <Story name="Multiple fields">
    <FormRow fields={['field1', 'field2']}>
      <Field
        fieldBox
        errors={['some error']}
        name="field1"
        label="Input field 1"
        helpText="Lorem ipsum"
      >
        <TextInput />
      </Field>
      <Field fieldBox name="field2" label="Input field 2" helpText="Lorem ipsum">
        <TextInput />
      </Field>
    </FormRow>
  </Story>
</Canvas>

### Props

<ArgsTable of={FormRow} />
