import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import ErrorList from './ErrorList';

<Meta
  title="Admin/Django/ErrorList"
  component={ErrorList}
  argTypes={{
    children: {table: {disable: true}},
  }}
/>

# Field

Wrapper for errors in the admin

export const Template = ({errors = [], ...args}) => <ErrorList {...args} children={errors} />;

## Usage

### List of error-strings

<Canvas>
  <Story
    name="List of error strings"
    args={{
      classNamePrefix: '',
      classNameModifier: '',
      errors: ['Error #1', 'Error #2'],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Empty errorlist

<Canvas>
  <Story
    name="No errors"
    args={{
      errors: [],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Single error

<Canvas>
  <Story
    name="Single error"
    args={{
      errors: 'single error',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable of={ErrorList} />
