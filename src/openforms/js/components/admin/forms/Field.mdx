import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs/blocks';

import Field from './Field';
import * as FieldStories from './Field.stories';

<Meta of={FieldStories} />

# Field

Wrapper for admin form fields. Note that the `name` prop is forwarded to the child (like
`TextInput`).

## Default usage

<Canvas of={FieldStories.Default} />

<ArgTypes of={Field} />

## Variants

### With validation errors

<Canvas of={FieldStories.HasErrors} />

### Disabled

<Canvas of={FieldStories.Disabled} />

### Required field

<Canvas of={FieldStories.Required} />
