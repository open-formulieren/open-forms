import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import Field from './Field';
import Fieldset from './Fieldset';
import FormRow from './FormRow';
import {TextInput} from './Inputs';

<Meta
  title="Admin/Django/Fieldset"
  component={Fieldset}
  argTypes={{
    children: {table: {disable: true}},
  }}
/>

# Fieldset

The Fieldset component is typically used to wrap one or more form rows,
or even content paragraphs.

If a title is provided, it will be styled with the primary color background.

Any additional props provided are forwarded to the underlying `fieldset` HTML
element.

export const Template = ({title, extraClassName=''}) => (
  <Fieldset title={title} extraClassName={extraClassName}>
    {/* typical example of nested content */}
    <FormRow>
      <Field name="Field" label="Input field" helpText="Lorem ipsum">
        <TextInput />
      </Field>
    </FormRow>
    <FormRow>
      <Field name="Field2" label="Input field2" helpText="Lorem ipsum">
        <TextInput />
      </Field>
    </FormRow>
  </Fieldset>
);

## Usage

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'The fieldset title',
      extraClassName: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />

### Without title

<Canvas>
  <Story
    name="Without title"
    args={{
      title: '',
      extraClassName: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
