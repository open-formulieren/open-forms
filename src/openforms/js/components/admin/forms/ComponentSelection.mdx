import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs/blocks';

import ComponentSelection from './ComponentSelection';
import * as ComponentSelectionStories from './ComponentSelection.stories';

<Meta of={ComponentSelectionStories} />

## Usage

Given a context of available (Formio) fields/components, display them in a dropdown.

Note that this component relies on the `FormContext` provider.

## Presentation

<Canvas of={ComponentSelectionStories.Default} />

### Props

<ArgTypes of={ComponentSelection} />
