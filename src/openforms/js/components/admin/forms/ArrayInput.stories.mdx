import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import ArrayInput from './ArrayInput';

<Meta title="Admin/Custom/ArrayInput" component={ArrayInput} />

# Field

Wrapper for admin arrayfield input

export const Template = args => <ArrayInput {...args} />;

## Presentation

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'field',
      inputType: 'text',
      values: ['foo', 'bar'],
      onChange: () => null,
      deleteConfirmationMessage: 'Are you sure you want to delete this?',
      addButtonMessage: 'Add more',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
