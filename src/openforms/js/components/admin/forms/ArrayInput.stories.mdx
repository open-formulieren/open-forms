import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import ArrayInput from './ArrayInput';

<Meta title="Django admin/ArrayInput" component={ArrayInput} />

# Field

Wrapper for admin arrayfield input

export const Template = args => <ArrayInput {...args} />;

## Presentation

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'Field',
      inputType: 'text',
      values: ['foo', 'bar'],
      onChange: (...args) => {
        null;
      },
      deleteConfirmationMessage: 'Are you sure you want to delete this?',
      addButtonMessage: 'Add more',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
