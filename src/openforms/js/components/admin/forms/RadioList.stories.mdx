import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import {Radio} from './Inputs';
import RadioList from './RadioList';

<Meta title="Admin/Django/RadioList" component={RadioList} />

# Field

Unordered list component

TODO: check if this is really a standard django component.

export const Template = args => <RadioList {...args} />;

## Presentation

<Canvas>
  <Story
    name="Default"
    args={{
      children: [<Radio name="foo" label="foo" />, <Radio name="bar" label="bar" />],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
