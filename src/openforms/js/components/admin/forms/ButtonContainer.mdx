import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs';

import ButtonContainer from './ButtonContainer';
import * as ButtonContainerStories from './ButtonContainer.stories';

<Meta of={ButtonContainerStories} />

## Usage

The button container is typically used in lists to add another item to the list.

<Canvas of={ButtonContainerStories.Default} />

## Props

<ArgTypes of={ButtonContainer} />
