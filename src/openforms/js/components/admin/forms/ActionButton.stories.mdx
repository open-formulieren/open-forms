import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import {ActionButton, AddAnotherAction, ContinueEditingAction, SubmitAction} from './ActionButton';

<Meta
  title="Admin/Django/ActionButton"
  component={ActionButton}
  argTypes={{
    component: {table: {disable: true}},
  }}
/>

# Base and variants of action button

The default Django admin action buttons

export const Template = ({component: Component, ...args}) => <Component {...args} />;

## Generic button

<Canvas>
  <Story
    name="ActionButton default"
    args={{
      component: ActionButton,
      name: '',
      text: 'Save',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ActionButton} />

## Pre-built variants

### Submit

<Canvas>
  <Story
    name="SubmitAction"
    args={{
      component: SubmitAction,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="SubmitAction" />

### Save and add another

<Canvas>
  <Story
    name="AddAnotherAction"
    args={{
      component: AddAnotherAction,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="AddAnotherAction" />

### Save and edit

<Canvas>
  <Story
    name="ContinueEditingAction"
    args={{
      component: ContinueEditingAction,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="ContinueEditingAction" />
