import {ArgTypes, Canvas, Meta} from '@storybook/addon-docs/blocks';

import ErrorList from './ErrorList';
import * as ErrorListStories from './ErrorList.stories';

<Meta of={ErrorListStories} />

# Field

Wrapper for errors in the admin

## Usage

### List of error-strings

<Canvas of={ErrorListStories.ListOfErrorStrings} />

### Empty errorlist

<Canvas of={ErrorListStories.NoErrors} />

### Single error

<Canvas of={ErrorListStories.SingleError} />

### Props

<ArgTypes of={ErrorList} />
