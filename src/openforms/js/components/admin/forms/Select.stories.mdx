import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import Select from './Select';

<Meta title="Admin/Django/Select" component={Select} />

# Field

Wrapper for select component

export const Template = args => <Select {...args} />;

## Presentation

{/* FIXME: Changing the value doesn't seem to work? */}

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'select',
      choices: [
        ['foo', 'Foo'],
        ['bar', 'Bar'],
      ],
      allowBlank: false,
      translateChoices: false,
      onChange: (...args) => {
        null;
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Default" />
