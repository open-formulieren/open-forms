import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import JsonWidget from './JsonWidget';

<Meta title="Admin/Custom/JsonWidget" component={JsonWidget} />

# Field

Input widget for form JsonLogic

export const Template = args => <JsonWidget {...args} />;

## Presentation

<Canvas>
  <Story
    name="Default"
    args={{
      name: 'JsonWidget',
      logic: {'==': [1, 1]},
      onChange: (...args) => {
        null;
      },
      cols: 40,
      isExpanded: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable of={JsonWidget} />
