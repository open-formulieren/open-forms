import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';

import ButtonContainer from './ButtonContainer';

<Meta title="Admin/Custom/ButtonContainer" component={ButtonContainer} />

## Usage

The button container is typically used in lists to add another item to the list.

export const Template = ({text, ...args}) => <ButtonContainer {...args}>{text}</ButtonContainer>;

<Canvas>
  <Story
    name="Default"
    args={{
      onClick: () => null,
      text: 'Add',
    }}
    argTypes={{
      children: {table: {disable: true}},
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={ButtonContainer} />
