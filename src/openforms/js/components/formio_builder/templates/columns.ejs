<div class="columns">
    {% ctx.component.columns.forEach(function(column, index) {%}
        <div class="column column--span-{{column.size}}" ref="{{ctx.columnKey}}">
    {{ctx.columnComponents[index]}}
        </div>
    {%}) %}
</div>
