# Generated by Django 4.2.16 on 2024-11-19 11:11

from django.db import migrations, models
from django.utils.text import slugify


def set_objects_api_group_config_identifier_from_name(apps, schema_editor):
    ObjectsAPIGroupConfig = apps.get_model("objects_api", "ObjectsAPIGroupConfig")

    def generate_unique_identifier(original_identifier, count=0):
        identifier = original_identifier + ("-" + str(count) if count else "")
        if not ObjectsAPIGroupConfig.objects.filter(identifier=identifier).exists():
            return identifier

        return generate_unique_identifier(original_identifier, count + 1)

    for row in ObjectsAPIGroupConfig.objects.all():
        candidate_slug = slugify(row.name)
        row.identifier = generate_unique_identifier(candidate_slug)
        row.save(update_fields=["identifier"])


class Migration(migrations.Migration):

    dependencies = [
        ("objects_api", "0001_initial"),
        # Related to https://github.com/open-formulieren/open-forms/issues/4654
        # Because the table was moved from registrations_objects_api to this app,
        # there needs to be a dependency here to ensure the table is actually created
        # when running tests
        ("registrations_objects_api", "0025_delete_objectsapigroupconfig"),
    ]

    operations = [
        migrations.AddField(
            model_name="objectsapigroupconfig",
            name="identifier",
            field=models.SlugField(
                blank=True,
                help_text="A unique, human-friendly identifier to identify this group.",
                verbose_name="identifier",
            ),
        ),
        migrations.RunPython(
            set_objects_api_group_config_identifier_from_name,
            reverse_code=migrations.RunPython.noop,
        ),
    ]
