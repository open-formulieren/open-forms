-c base.txt
-r base.txt

# Dependencies only relevant for (unit) testing
coverage
django-simple-certmanager[testutils]
django-webtest
factory-boy
freezegun
maykin-common[axes,mfa,pdf,vcr]
playwright
pyquery  # integrates with webtest
requests-mock
tenacity
tblib
hypothesis  # property-based testing
zgw-consumers[testutils]

# Code formatting
ruff

# Build tooling
# setup.py install fallback is deprecated, building wheels locally and caching them
# is the most performant way to install dependencies
wheel
